1.0 set up user auth
1.1 user registration and login (username, email, full name)
1.2 user management screen (show all users, allow searching users, delete user, block email, block username, trigger password reset, set password, designate admin)
1.2.1 password reset screen on login using current password
1.2.2 admin only settings (providers, per provider daily token limits
1.3 user statistics (token use, audits run, successes, failures with the full audit trace being able to drop down)
1.4 overall token use/other stats
